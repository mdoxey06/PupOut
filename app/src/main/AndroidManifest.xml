<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="course.examples.pupout">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeScreen"
            android:label="PupOut" />
        <activity
            android:name=".Matches"
            android:label="Matches" />
        <activity
            android:name=".Redeem"
            android:label="Redeem Points" />
        <activity
            android:name=".EditProfile"
            android:label="Edit Profile" />
        <activity
            android:name=".MatchFilters"
            android:label="Edit Filters" />
        <activity
            android:name=".ExchangeScreen"
            android:label="Redeem Points" />
        <activity
            android:name=".Redeem2"
            android:label="Points Redeemed!" />
        <activity
            android:name=".WelcomePage"
            android:label="PupOut"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".InitialSignUp"
            android:label="@string/app_name" />
        <activity
            android:name=".SecondSignUp"
            android:label="@string/app_name" />
        <activity
            android:name=".ThirdSignUp"
            android:label="@string/app_name" />
        <activity
            android:name=".FourthSignUp"
            android:label="@string/app_name" />
        <activity
            android:name=".Completed"
            android:label="@string/app_name" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".DogPref"
            android:label="@string/title_activity_dog_pref"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>